// Re-export from MapData.ts for backwards compatibility
// All regional data with official GeoJSON boundaries is now in MapData.ts

export {
  regionalData,
  regionColors,
  type TouristSpot,
  type RegionData
} from './MapData';
